apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    # Container-to-container name on the compose network, not localhost -
    # Grafana and Prometheus are both containers here, unlike the broker.
